from .cube import Cube
from .button import Button
from .text import Text

# TODO: add a group object, which will behave like objects in the scene, but will be a container for other objects
#  which will make it convenient to move, rotate, etc. all objects in the group at once, and rotate them around the
#  group's center
